{% extends 'base.html.twig' %}

{% block title %}Nouveau mot de passe{% endblock %}

{% block body %}

<link rel="stylesheet" href="{{ asset('css/resetPassword.css') }}">


        <div class="container">
            <div class="heading">Définir un nouveau mot de passe</div>

            {{ form_start(form, { 'attr': {'class': 'form'} }) }}

            <div class="form-group">

                {{ form_widget(form.newPassword, { 'attr': { 'class': 'input-field' } }) }}
            </div>

            <div class="btn-container">
                <button type="submit" class="btn">Reinitialiser le mot de passe</button>
            </div>

            {{ form_end(form, {'attr': {'class': 'hidden'}}) }}


        </div>



{% endblock %}